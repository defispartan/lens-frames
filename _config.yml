remote_theme: pages-themes/tactile@v0.2.0
plugins:
  - jekyll-remote-theme # add this line to the plugins list if you already have one
title: [Lens Frames Specification]
description:
  [
    An implementation of the Open Frames standard to define structure of authenticated frame applications for Lens Protocol,
  ]
